/**
 * Définition des types pour toutes les routes API
 * 
 * Ce fichier est généré automatiquement et contient les types pour toutes les routes API.
 * Il importe les gestionnaires de route pour chaque modèle et les exporte sous forme d'objet.
 * 
 * Ne pas modifier ce fichier manuellement, il sera écrasé lors de la prochaine génération.
 */

import { NextRequest, NextResponse } from "next/server";

// Type pour les gestionnaires de route
type RouteHandler = {
  GET?: (request: NextRequest) => Promise<NextResponse>;
  POST?: (request: NextRequest) => Promise<NextResponse>;
  PUT?: (request: NextRequest) => Promise<NextResponse>;
  DELETE?: (request: NextRequest) => Promise<NextResponse>;
  PATCH?: (request: NextRequest) => Promise<NextResponse>;
};

{{#each models}}
import { Select{{this.name}}AmountProps, Select{{this.name}}ListProps, Select{{this.name}}Props } from "@actions/types/{{this.name}}";
import { Select{{this.name}}AmountResponse } from "@app/api/{{this.namePlural}}/count/route";
import { Select{{this.name}}ListResponse } from "@app/api/{{this.namePlural}}/route";
import { Select{{this.name}}Response } from "@app/api/{{this.namePlural}}/unique/route";
// Import des gestionnaires pour {{this.name}}
import * as {{this.name}}Main from "./{{this.namePlural}}/route";
import * as {{this.name}}Unique from "./{{this.namePlural}}/unique/route";
import * as {{this.name}}Count from "./{{this.namePlural}}/count/route";
{{/each}}

export type Routes = {
  {{#each models}}
  {{this.namePlural}}: {
    main: RouteHandler;
    unique: RouteHandler;
    count: RouteHandler;
  };
  {{#unless @last}}
  {{/unless}}
  {{/each}}
};

// Export des routes
export const {{#each models}}
{{this.namePlural}} = {
  main: {{this.name}}Main,
  unique: {{this.name}}Unique,
  count: {{this.name}}Count,
};
{{#unless @last}}
{{/unless}}
{{/each}}
